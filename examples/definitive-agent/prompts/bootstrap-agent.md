# Bootstrap {{agent-name}}

Finish the declared agent setup from the configured working root. Confirm the
role-based repository credential is available at its declared destination,
apply the repository's checked-in setup instructions, and report the resulting
health checks. Do not change the Codex permission profile or expose credential
material in output.
