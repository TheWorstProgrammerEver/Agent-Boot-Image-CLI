The New Agent Boot CLI project, containing:
1. TypeScript package for defining agent setup descriptions as .ts files.
2. Agent Image CLI which ingests agent setup descriptions and produces a boot image on to a specified destination.
3. Runner process (included in boot image) which executes steps defined in agent description.
